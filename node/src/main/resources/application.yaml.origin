server:
  port: 8085

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: username
    password: password

  datasource:
    url: jdbc:postgresql://localhost:5433/db_name
    username: username
    password: password

  flyway:
    schemas:
      - public